name: my-workflow

on:
  push:
    branches: [$default-branch]
  pull_request:
    branches: [$default-branch]

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@V4
      
      - name: Run a one line script
        run: echo Hello, Naresh!
